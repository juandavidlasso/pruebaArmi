import { createTheme } from '@mui/material/styles';

export const ligthTheme = createTheme({
    palette: {
        mode: 'light',
        background: {
            default: '#FFFFFF',
        },
    },
});
